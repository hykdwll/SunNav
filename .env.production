# Vercel生产环境配置
# 这些变量将在Vercel部署时自动使用

# 前端API URL - Vercel会自动使用当前域名
REACT_APP_API_URL=/api

# JWT密钥 - 从Vercel环境变量获取
JWT_SECRET=${JWT_SECRET}

# 数据库配置 - 从Vercel环境变量获取
DB_HOST=${DB_HOST}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=${DB_NAME}
DB_PORT=${DB_PORT}

# Node环境
NODE_ENV=production